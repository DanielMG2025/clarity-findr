// Clinics module — shared clinic primitives (card + types). Mock data lives in
// pages until we wire to Supabase queries directly.
export { ClinicCardV2, type ClinicCardData } from "@/components/shared/ClinicCardV2";
